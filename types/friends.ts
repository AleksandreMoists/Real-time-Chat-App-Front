export interface FriendsProps {
    id: number;
    mail?: string; // It must be changed to neccessary
    name: string;
    lastname: string;
    avatar: string;
    status: string;
    pending: boolean;
}